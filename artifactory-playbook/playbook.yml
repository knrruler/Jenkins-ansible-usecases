---
- hosts: artifactory
  become: yes
  become_method: sudo


  vars_files:
    - defaults/main.yml
    - vars/main.yml

  vars:
    - artifactory_database: postgres
    - base_postgres_user: postgres
    - base_postgres_pass: postgres

  roles:
    - role: artifactory-playbook
